{
  "name": "flatten-directory-structure",
  "version": "0.0.1",
  "description": "Flatten a directory structure to a single folder, renaming any duplicate files accordingly",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin Drew",
  "license": "ISC",
  "dependencies": {
    "mv": "^2.1.1"
  }
}
